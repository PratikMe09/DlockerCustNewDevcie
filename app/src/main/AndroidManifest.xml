<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.trustonic.overlaynewdevice">

    <!-- Required permissions for Device Admin -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Permissions for IMEI detection and image selection -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OverlayNewdevice"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name="com.d.locker.lock.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OverlayNewdevice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SIM Toolkit Dummy Screen -->
        <activity
            android:name="com.d.locker.lock.activities.SimToolkitActivity"
            android:exported="false"
            android:label="SIM Toolkit"
            android:theme="@style/Theme.OverlayNewdevice"
            android:screenOrientation="portrait" />

        <!-- 
        Device Admin Receiver - CRITICAL COMPONENT
        Package: com.trustonic.overlaynewdevice
        Class: com.d.locker.lock.receivers.MyDeviceAdminReceiver
        Full Component: com.trustonic.overlaynewdevice/com.d.locker.lock.receivers.MyDeviceAdminReceiver
        -->
        <!-- 
        Device Admin Receiver - CRITICAL COMPONENT
        Package: com.trustonic.overlaynewdevice
        Class: com.d.locker.lock.receivers.MyDeviceAdminReceiver
        Full Component: com.trustonic.overlaynewdevice/com.d.locker.lock.receivers.MyDeviceAdminReceiver
        -->
        <receiver
            android:name="com.d.locker.lock.receivers.MyDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">

            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />

        </receiver>

        <!-- FileProvider for camera images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>



        <!-- Lock Screen Activity -->
        <activity
            android:name="com.d.locker.lock.activities.LockActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.OverlayNewdevice"
            android:screenOrientation="portrait" />

        <!-- Firebase Messaging Service -->
        <service
            android:name="com.d.locker.lock.services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>